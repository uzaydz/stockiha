legacy-peer-deps=true
engine-strict=false

# ⚡ حل مشكلة Electron مع pnpm
# pnpm يستخدم symlinks بشكل افتراضي، مما يسبب مشاكل مع Electron
# هذا الإعداد يجعل node_modules flat مثل npm
node-linker=hoisted

# السماح بتشغيل Electron build scripts
shamefully-hoist=true
