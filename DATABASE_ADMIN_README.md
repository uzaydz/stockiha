# 📊 مستكشف قاعدة البيانات المحلية (Database Admin)

## نظرة عامة

صفحة **مستكشف قاعدة البيانات** هي أداة قوية لعرض وإدارة جميع البيانات المخزنة محلياً في قاعدة بيانات SQLite داخل تطبيق Electron.

---

## 🌟 الميزات

### 1. عرض جميع الجداول
- قائمة كاملة بجميع الجداول في قاعدة البيانات
- بحث سريع في أسماء الجداول
- عرض عدد السجلات لكل جدول

### 2. عرض بنية الجداول
- عرض جميع الأعمدة وأنواع بياناتها
- معرفة المفاتيح الأساسية (Primary Keys)
- القيم الافتراضية والقيود (Constraints)

### 3. تصفح البيانات
- عرض السجلات مع pagination
- دعم الترتيب والفلترة
- عرض 50 سجل في كل صفحة

### 4. معلومات قاعدة البيانات
- حجم قاعدة البيانات
- عدد الجداول الإجمالي
- إحصائيات شاملة

---

## 📋 الجداول المتاحة

تحتوي قاعدة البيانات المحلية على الجداول التالية:

### 🛒 **المنتجات والمخزون**
- `products` - المنتجات
- `categories` - الفئات
- `inventory` - المخزون
- `transactions` - معاملات المخزون

### 💰 **المبيعات والطلبات**
- `pos_orders` - طلبات نقطة البيع
- `pos_order_items` - عناصر الطلبات
- `invoices` - الفواتير
- `invoice_items` - عناصر الفواتير

### 👥 **العملاء والديون**
- `customers` - العملاء
- `addresses` - العناوين
- `customer_debts` - ديون العملاء

### 🔧 **الخدمات والإصلاحات**
- `repair_orders` - طلبات الإصلاح
- `repair_images` - صور الإصلاح

### 👨‍💼 **إدارة الموظفين**
- `staff_pins` - رموز PIN الموظفين
- `work_sessions` - جلسات العمل

### 🔄 **المزامنة**
- `sync_queue` - صف المزامنة
- `organization_subscriptions` - اشتراكات المؤسسات

### 📦 **المرتجعات والخسائر**
- `product_returns` - إرجاعات المنتجات
- `return_items` - عناصر الإرجاع
- `loss_declarations` - إعلانات الخسائر
- `loss_items` - عناصر الخسارة

### ⚙️ **الإعدادات**
- `pos_settings` - إعدادات نقطة البيع

---

## 🚀 كيفية الوصول

### الخطوة 1: تشغيل التطبيق في وضع Electron
```bash
npm run desktop:dev
```

### الخطوة 2: الوصول إلى الصفحة
في المتصفح أو التطبيق، انتقل إلى:
```
/database-admin
```

**ملاحظة:** هذه الصفحة متاحة **فقط** في تطبيق Electron (Desktop)، ولن تعمل في المتصفح العادي.

---

## 📸 لقطات الشاشة

### 📋 قائمة الجداول
- عرض جميع الجداول مع إمكانية البحث
- عرض عدد السجلات لكل جدول

### 📊 عرض البيانات
- جدول تفاعلي لعرض البيانات
- Pagination للتنقل بين الصفحات
- دعم الترتيب حسب الأعمدة

### 🏗️ بنية الجدول
- عرض الأعمدة وأنواعها
- المفاتيح والقيود

---

## 🔒 الأمان

- ✅ الصفحة محمية ومتاحة فقط في Electron
- ✅ لا يمكن الوصول إليها من المتصفح
- ✅ استعلامات SQL آمنة ومحمية من SQL Injection
- ✅ التحقق من صحة المدخلات

---

## 🛠️ التطوير

### هيكل الكود

```
src/pages/DatabaseAdmin.tsx          # الصفحة الرئيسية
electron/main.cjs                    # IPC Handlers
electron/preload.secure.cjs          # Electron API Bridge
electron/sqliteManager.cjs           # مدير قاعدة البيانات
```

### IPC Handlers المضافة

```javascript
// الحصول على قائمة الجداول
window.electronAPI.db.getTables()

// الحصول على معلومات جدول
window.electronAPI.db.getTableInfo(tableName)

// الحصول على عدد السجلات
window.electronAPI.db.getTableCount(tableName)

// الحصول على بيانات الجدول
window.electronAPI.db.getTableData(tableName, { limit, offset, orderBy, orderDir })
```

---

## 📝 التحسينات المستقبلية

- [ ] تصدير البيانات إلى CSV/JSON
- [ ] تحرير السجلات مباشرة
- [ ] تنفيذ استعلامات SQL مخصصة
- [ ] عرض الرسوم البيانية والإحصائيات
- [ ] البحث المتقدم في البيانات
- [ ] نسخ احتياطي واستعادة البيانات

---

## 🐛 التبليغ عن الأخطاء

في حالة مواجهة أي مشاكل، يرجى:
1. التحقق من وحدة التحكم (Console) للأخطاء
2. التأكد من تشغيل التطبيق في وضع Electron
3. التأكد من تهيئة قاعدة البيانات

---

## 📞 الدعم

للحصول على المساعدة:
- افتح issue في GitHub
- راجع التوثيق الرئيسي
- تواصل مع فريق التطوير

---

**تم التطوير بواسطة:** فريق Stockiha
**التاريخ:** 2025-11-05
**الإصدار:** 1.0.0
