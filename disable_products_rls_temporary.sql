-- ==============================================================
-- حل طارئ: تعطيل RLS مؤقتاً لجدول المنتجات
-- استخدم هذا فقط إذا لم يعمل الحل المبسط
-- ==============================================================

-- تعطيل RLS مؤقتاً
ALTER TABLE public.products DISABLE ROW LEVEL SECURITY;

-- رسالة تحذيرية
SELECT 
    '⚠️ تحذير: تم تعطيل Row Level Security لجدول المنتجات' as warning,
    'هذا حل مؤقت فقط - يجب إعادة تفعيله لاحقاً' as note,
    'جرب إنشاء منتج الآن' as action;

-- ==============================================================
-- لإعادة تفعيل RLS لاحقاً، استخدم:
-- ALTER TABLE public.products ENABLE ROW LEVEL SECURITY;
-- ============================================================== 