# 🔒 Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local or any file containing real secrets!

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Development Settings
NODE_ENV=development
VITE_NODE_ENV=development

# Domain Settings
VITE_APP_DOMAIN=yourdomain.com
VITE_SUPPORTED_DOMAINS=yourdomain.com,localhost

# API Settings
VITE_API_URL=/api
VITE_DOMAIN_PROXY=your-proxy-domain.com

# Deployment Platform
VITE_DEPLOYMENT_PLATFORM=local

# Security Settings
VITE_DISABLE_CSP=false

# 🚨 IMPORTANT SECURITY NOTES:
# 1. Never commit files with real values
# 2. Use Cloudflare Pages environment variables for production
# 3. All VITE_ variables are exposed to the browser
# 4. Never put sensitive keys in VITE_ variables
