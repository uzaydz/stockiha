# ═══════════════════════════════════════════════════════════════════════════════
# 🧪 POWERSYNC SYNC RULES - اختبار بـ user_id
# ═══════════════════════════════════════════════════════════════════════════════
# انسخ هذا المحتوى إلى PowerSync Dashboard → Sync Rules → Deploy
# ═══════════════════════════════════════════════════════════════════════════════

bucket_definitions:
  # Bucket للمستخدم - يستخدم sub من JWT (دائماً موجود)
  user_bucket:
    parameters:
      - SELECT token_parameters.user_id as uid

    data:
      # جدول المستخدمين - يجلب بيانات المستخدم الحالي فقط
      - SELECT id, email, name, organization_id, role, created_at
        FROM users
        WHERE id = bucket.uid

