# تقرير حالة بنية "Offline First" (العمل دون اتصال)

هذا التقرير يلخص ما تم إنجازه، ما تبقى من مهام، والمشاكل الحالية في مشروع تحويل التطبيق ليعمل بشكل كامل دون اتصال بالإنترنت مع الاعتماد الأساسي على SQLite.

## ✅ ما تم إنجازه (المكتمل)

1.  **تسجيل الدخول دون اتصال (Offline Login):**
    *   تم تنفيذ آلية لتخزين بيانات الاعتماد (كلمة المرور) بشكل آمن (Hashed) محلياً.
    *   تعديل `LoginForm.tsx` للتحقق من البيانات محلياً عند عدم توفر الإنترنت.
    *   تخزين والتحقق من حالة الاشتراك (Subscription) محلياً لمنع الوصول إذا كان الاشتراك منتهياً حتى بدون إنترنت.

2.  **إدارة الاشتراكات محلياً:**
    *   إنشاء `offlineSubscriptionService.ts` لجلب وتخزين والتحقق من صلاحية الاشتراك محلياً.
    *   إضافة واجهة `LocalSubscription` في قاعدة البيانات المحلية.

3.  **تخزين الصور محلياً (Image Caching):**
    *   إنشاء `imageSyncService.ts` لتنزيل الصور وتخزينها في مسار محلي آمن.
    *   إضافة دعم IPC في Electron (`main.cjs`, `preload.cjs`) لتنزيل الصور وحفظها على القرص.
    *   تحديث `localProductService.ts` لاستخدام المسارات المحلية للصور عند عرض المنتجات.

4.  **محرك المزامنة (Sync Engine):**
    *   تحديث `SyncEngine.ts` ليشمل عملية `initialSync` (المزامنة الأولية) الشاملة.
    *   المزامنة الأولية تشمل الآن: المنتجات، العملاء، الصور، الفواتير، المرتجعات، والتصاريح بالخسائر.
    *   تحسين `SyncEngine` ليعمل بشكل متوازي أو تسلسلي حسب الإعدادات.

5.  **خدمات المزامنة (Sync Services):**
    *   تم الانتهاء من كتابة خدمات المزامنة للكيانات التالية:
        *   `syncProductReturns.ts`: لمزامنة إرجاع المنتجات (رفع وسحب).
        *   `syncLossDeclarations.ts`: لمزامنة التصريح بالخسائر.
        *   `syncInvoices.ts`: لمزامنة الفواتير.
    *   جميع الخدمات تدعم مبدأ "Server Wins" في حال التضارب، وتقوم بتحديث الحالة المحلية (synced/pending).

6.  **قاعدة البيانات (SQLite & Adapter):**
    *   تحديث `sqliteAPI.ts` و `sqliteManager.cjs` لدعم العمليات الجديدة.
    *   إضافة دالة `bulkUpsert` في `SQLiteManager` لتحسين أداء المزامنة الأولية (إدخال البيانات دفعة واحدة).
    *   تحديث `dbAdapter.ts` ليعطي الأولوية لـ SQLite في بيئة Electron ويحل مشكلة توافق الأنواع.

## ⏳ المتبقي (Required)

1.  **تحديث واجهات المستخدم (UI Integration):**
    *   يجب تحديث الصفحات التالية لتعتمد كلياً على الخدمات المحلية وتعرض حالة المزامنة:
        *   `src/pages/dashboard/CustomerDebts.tsx` (ديون العملاء)
        *   `src/pages/returns/ProductReturns.tsx` (المرتجعات)
        *   `src/pages/losses/LossDeclarations.tsx` (الخسائر)
        *   `src/pages/dashboard/Invoices.tsx` (الفواتير)
    *   صفحة `Customers.tsx` تم تحديثها جزئياً ولكن تحتاج لمراجعة نهائية للتأكد من عمل الفلاتر والبحث أوفلاين بكفاءة.

2.  **لوحة تحكم المزامنة (Debug Panel):**
    *   إنشاء `src/utils/syncLogger.ts` لتسجيل عمليات المزامنة والأخطاء بشكل مركزي.
    *   إنشاء صفحة `src/pages/debug/SyncPanel.tsx` للمطورين لمراقبة حالة المزامنة، رؤية السجلات، وإجبار المزامنة يدوياً.

3.  **تنظيف الكود والتحسين:**
    *   مراجعة `LoginForm.tsx` وإزالة رسائل الـ Debugging الكثيرة (`console.log`).
    *   التأكد من أن جميع الـ Hooks (مثل `useUnifiedPOSData`) تستخدم المسار الصحيح للبيانات (SQLite عبر Adapter) ولا تعتمد على Dexie بشكل مباشر في Electron.

## ⚠️ المشاكل والتحديات الحالية (Issues)

1.  **خطأ Lint في الأنواع (Type Error):**
    *   كان هناك خطأ: `Type 'string' is not assignable to type '"sqlite" | "indexeddb"'` في `localDb.ts`.
    *   **الحالة:** تم التعامل معه بتحديد نوع الإرجاع في `dbAdapter.ts`، ولكن يجب التأكد من اختفاء الخطأ تماماً عند بناء المشروع.

2.  **تعقيد البنية المزدوجة (Dual DB Complexity):**
    *   التطبيق يدعم SQLite (لـ Electron) و Dexie/IndexedDB (للمتصفح). الحفاظ على تطابق السلوك بين الاثنين يتطلب جهداً إضافياً في `dbAdapter`. أي ميزة جديدة يجب إضافتها في المكانين.

3.  **أداء المزامنة الأولية:**
    *   تنزيل آلاف الصور أثناء المزامنة الأولية قد يستغرق وقتاً ويستهلك موارد. تم تحسين ذلك باستخدام `bulkUpsert` للبيانات، ولكن الصور لا تزال تُحمل بشكل فردي (وإن كان بشكل متوازي).

4.  **استراتيجية فض النزاع (Conflict Resolution):**
    *   الاستراتيجية الحالية هي "Server Wins" (السيرفر هو المصدر الموثوق). هذا يعني أنه إذا عدل المستخدم بيانات محلياً وعدلها شخص آخر على السيرفر، قد تضيع التعديلات المحلية عند المزامنة. هذا مقبول حالياً ولكنه قد يحتاج لتحسين مستقبلاً (مثل دمج البيانات أو إشعار المستخدم).

## 🚀 الخطوات القادمة المقترحة

1.  البدء فوراً بتحديث صفحات الـ UI المتبقية (`CustomerDebts`, `ProductReturns`, etc.) لربطها بالخدمات الجديدة.
2.  إنشاء لوحة `SyncPanel` لتسهيل اختبار ومراقبة عملية المزامنة.
3.  إجراء اختبار شامل لسيناريو "انقطاع الإنترنت -> العمل -> عودة الإنترنت" للتأكد من عدم ضياع أي بيانات.
