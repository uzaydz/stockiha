# تحويل جميع أسماء الأعمدة من camelCase إلى snake_case

## ✅ ما تم إنجازه

### v59: تحويل جميع الأعمدة إلى snake_case

تم تحديث جميع تعريفات الجداول لاستخدام `snake_case` فقط بدلاً من `camelCase`:

#### الجداول المحدثة:

1. **work_sessions**
   - ✅ `sync_status` (كان `syncStatus`)
   - ✅ `pending_operation` (كان `pendingOperation`)

2. **staff_work_sessions**
   - ✅ جميع الأعمدة تستخدم `snake_case` من البداية

3. **expense_categories**
   - ✅ `pending_operation` (كان `pendingOperation`)

4. **expenses**
   - ✅ `pending_operation` (كان `pendingOperation`)

5. **recurring_expenses**
   - ✅ `pending_operation` (كان `pendingOperation`)

## 📋 الأعمدة الموحدة

جميع أعمدة المزامنة الآن تستخدم `snake_case`:

- ✅ `synced` (INTEGER)
- ✅ `sync_status` (TEXT)
- ✅ `pending_operation` (TEXT)
- ✅ `local_updated_at` (TEXT)
- ✅ `local_created_at` (TEXT)
- ✅ `server_created_at` (TEXT)
- ✅ `last_sync_attempt` (TEXT)
- ✅ `created_at` (TEXT)
- ✅ `updated_at` (TEXT)
- ✅ `organization_id` (TEXT)

## ⚠️ ملاحظات مهمة

### الأعمدة camelCase القديمة
- الأعمدة camelCase القديمة (`syncStatus`, `pendingOperation`, etc.) قد تظل موجودة في قواعد البيانات الموجودة
- SQLite لا يدعم `DROP COLUMN` مباشرة في الإصدارات القديمة
- هذه الأعمدة ستُتجاهل تلقائياً عند القراءة لأن الكود الآن يستخدم `snake_case` فقط
- يمكن إزالتها يدوياً لاحقاً إذا لزم الأمر

### التوافق العكسي
- الكود القديم الذي يستخدم camelCase لن يعمل مع الجداول الجديدة
- يجب تحديث جميع الكود لاستخدام `snake_case` فقط

## 🔧 التغييرات المطبقة

### في tauriSchema.ts
1. ✅ تحديث SCHEMA_VERSION إلى 59
2. ✅ تحويل جميع أعمدة `pendingOperation` إلى `pending_operation`
3. ✅ إزالة إضافة أعمدة camelCase القديمة من `work_sessions`
4. ✅ إضافة منطق migration للتحقق من استخدام snake_case فقط

## 📊 حالة الجداول

| الجدول | الحالة | ملاحظات |
|--------|--------|---------|
| `work_sessions` | ✅ محدث | جميع الأعمدة snake_case |
| `staff_work_sessions` | ✅ محدث | جميع الأعمدة snake_case |
| `expense_categories` | ✅ محدث | جميع الأعمدة snake_case |
| `expenses` | ✅ محدث | جميع الأعمدة snake_case |
| `recurring_expenses` | ✅ محدث | جميع الأعمدة snake_case |

## ✅ الخلاصة

تم تحويل جميع أسماء الأعمدة من camelCase إلى snake_case:
- ✅ جميع تعريفات CREATE TABLE تستخدم snake_case فقط
- ✅ جميع أعمدة المزامنة موحدة
- ✅ لا توجد أعمدة camelCase جديدة يتم إضافتها
- ⚠️ الأعمدة camelCase القديمة قد تظل موجودة لكن لن تُستخدم

الخطوة التالية: التأكد من أن جميع الكود الذي يقرأ/يكتب في قاعدة البيانات يستخدم snake_case فقط.

























